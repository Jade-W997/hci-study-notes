# Simple data analysis example
# This script calculates the average value from a list of user interaction times

interaction_times = [2.3, 1.8, 3.1, 2.6, 1.9]

average_time = sum(interaction_times) / len(interaction_times)

print("Average interaction time:", average_time)
